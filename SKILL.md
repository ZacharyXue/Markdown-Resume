---
name: resume-builder
description: "Convert raw ideas into polished resume content, manage project files, and generate targeted HTML resumes."
version: 1.2.0
platforms: [linux]
metadata:
  hermes:
    tags: [resume, markdown, html, career]
---

# 简历构建器

将用户的原始描述转换为精炼的简历语言，管理独立项目文件，并按需生成 HTML 简历。

## 架构

简历采用 **拆分模式**：基本信息与项目经历分离。

```
Markdown-Resume/
├── dist/                        ← 独立私有 git 仓库
│   ├── .git/                    ← 本地 only，无 remote
│   ├── resume-base.md           ← 基本信息（头/教育/工作/技能）
│   ├── projects/
│   │   ├── bytedance/
│   │   │   ├── gitlab-ci-platform.md        ← CI/CD 平台 + 消息中台
│   │   │   └── model-testing-deployment.md  ← 模型测试 + 芯片部署
│   │   └── huawei/
│   │       └── storage-server-testing.md
│   ├── Resume.html              ← 生成产物
│   └── Resume.pdf
├── scripts/generate_resume.py   ← 构建脚本
├── resume.css                   ← 样式
└── assets/                      ← SVG 图标
```

- `dist/` 目录已 gitignored（父仓库），真实信息不进入公开仓库
- `dist/` 本身是一个独立的私有 git 仓库，用于版本追溯
- **不推送、不创建远端连接**，改动后 `cd dist && git add -A && git commit`
- `dist/resume-base.md` 包含 `<!-- PROJECTS -->` 标记，脚本在此处注入选中的项目内容

---

## 工作流一：将原始描述转为简历语言

用户可能这样描述：

> 我最近在做 CI/CD 平台，用的是 GitLab，搞了 100 多台机器，给公司 200 多个开发用

你需要将其转换为：

> - 从 0 搭建并运维 100+ 节点、15000 CPU 的 GitLab CI 集群，服务 200+ 开发者

### 转换流程

1. 用户描述一个项目 → 你追问关键信息（量化数据、技术栈、时间范围）
2. 生成润色后的 markdown 片段，展示给用户确认
3. 用户确认后，保存到 `dist/projects/<公司>/<slug>.md`
4. `cd dist && git add -A && git commit -m "add: <project-name>"`

---

## 润色方法论

### 核心公式

#### 公式一：X-Y-Z 公式（Google 方法）

```
"Accomplished [X] as measured by [Y] by doing [Z]"
"实现了 [X]，以 [Y] 衡量，通过 [Z] 做到"
```

| 元素 | 含义 | 示例 |
|------|------|------|
| X | 你取得了什么成果 | 集群可用率提升至 99.8% |
| Y | 如何衡量这个成果 | 从 97.2% 提升至 99.8% |
| Z | 你做了什么动作 | 优化 PostgreSQL 查询并引入 Redis 缓存 |

**示例：**

> ❌ BEFORE: "负责优化后端性能"
>
> ✅ AFTER: "将 API 延迟降低 60%（500ms → 200ms），支撑 50 万日活用户，通过优化 PostgreSQL 查询和引入 Redis 缓存实现"

#### 公式二：STAR 法则

```
S (Situation)  →  背景/上下文是什么？
T (Task)       →  你需要完成什么？
A (Action)     →  你具体做了什么？
R (Result)     →  产生了什么可衡量的结果？
```

**完整 STAR → 简历一句压缩：**

> S: 接手了未达标研发效能团队
> T: 需要在 Q1 翻转到达标
> A: 引入 CI/CD 流水线 + 制定代码审查规范
> R: 部署频率提升 3 倍，故障恢复时间从 4h 降至 30min
>
> → **简历一句**: "通过引入 CI/CD 流水线和代码审查规范，将团队部署频率提升 3 倍、故障恢复时间从 4 小时压缩至 30 分钟"

#### 公式三：技术角色专属公式

```
[强动词] + [技术方案] + [规模/影响] + [使用的技术栈]
```

> ✅ "设计并实现认证微服务（OAuth 2.0 + JWT），服务 200 万用户，零安全事故"
>
> ✅ "搭建实时通知系统（WebSocket + AWS SNS），日均处理 100 万条消息，送达率 99.9%"

---

### 动词分级体系

选择动词时，**按能量级别**而不是按"意思相近"来选：

#### L1 — 领导/推动级（最有力）

`主导` `推动` `发起` `带领` `引领` `驱动`

> 用于体现主动性、ownership。你是那个"让事情发生"的人。

#### L2 — 创建/构建级

`搭建` `开发` `设计` `构建` `架构` `创立`

> 用于从 0 到 1 或重大重构。强调你创造了什么新东西。

#### L3 — 优化/提升级

`优化` `缩短` `降低` `提升` `加速` `自动化`

> 用于改进已有系统。必须有 before/after 对比。

#### L4 — 解决/分析级

`解决` `定位` `排查` `分析` `诊断`

> 用于故障处理、问题攻关。强调问题规模和解决效率。

#### 🚫 禁用动词

| 禁用 | 原因 | 替换为 |
|------|------|--------|
| 负责 | 描述职责，非成就 | 主导 / 搭建 / 交付 |
| 参与 | 模糊，无个人贡献感 | 具体动作（开发了 / 实现了） |
| 协助 | 像配角 | 配合 / 支撑 + 量化 |
| 帮忙 | 口语化，无专业感 | 实际动作 |
| 做了 / 搞了 | 太随意 | 具体动词 |

---

### 量化策略

**黄金法则：每个要点至少包含一个数字。**

#### 五类指标

| 类别 | 工程示例 | 通用示例 |
|------|----------|----------|
| **规模** | 100+ 节点、200 万用户、10K QPS | 管理 50 个客户、带领 12 人团队 |
| **时间** | 部署从 30min → 5min | 提前 2 个月交付、响应 < 2h |
| **百分比** | 可用性 99.99%、延迟降 60% | 效率提升 40%、留存率 95% |
| **成本** | 年省 $20 万云成本 | 预算节省 30% |
| **对比** | 准确率 85% → 98% | 排名 #2/45 |

#### 工程角色专属指标清单

- **规模**: 节点数、CPU 核数、日活用户、QPS、数据量(TB)、服务数
- **性能**: 延迟(P50/P99)、吞吐量、启动时间、编译时间
- **可靠性**: 可用性(几个9)、MTTR、MTBF、错误率
- **效率**: 部署频率、Lead Time、人时节省、构建时间
- **业务**: 支撑收入、转化率提升、客户数

#### 当你说"我没有数字"时

**方法一：保守估算**

> 不确定是 60% → 写"~50%"或"50%+"
> 不确定是 100 个 → 写"80+"或"近百"

**方法二：量化投入（output 未知时量化 input）**

> "分析了 500+ 条日志"、"编写了 2 万行自动化脚本"、"覆盖 30+ 模型"

**方法三：时间反推**

> 每天处理 30 个 ticket × 250 天 = "年处理 7500+ 个 ticket"
> 每周部署 5 次 × 50 周 = "年部署 250+ 次"

**方法四：用对比代替绝对数字**

> "准确率从 85% 提升至 98%"（哪怕不知道绝对准确率，知道提升了多少也行）

---

### 常见错误与修正

| # | 错误 | 示例 | 修正 |
|---|------|------|------|
| 1 | **被动/职责语言** | "负责管理 CI 集群" | "主导 100+ 节点 CI 集群建设" |
| 2 | **无数字** | "提升了系统性能" | "将 API 延迟从 500ms 降至 200ms" |
| 3 | **职责当成就** | "日常维护测试环境" | "维护 30+ 套测试环境，可用性 99.5%" |
| 4 | **太笼统** | "和各个团队合作" | "协同 5 个跨职能团队(共 50+ 人)" |
| 5 | **太长** | 一句话 80 字还带拐弯 | 控制在 40-60 字，一个要点只说一件事 |
| 6 | **技术名词堆砌** | "用 K8s、Docker、Helm、Istio..." | 选 1-2 个核心技术，其余放技术栈行 |
| 7 | **团队功劳个人化** | 把整个团队的产出写成自己的 | 写清楚"带领 X 人团队"或"负责 XX 模块" |
| 8 | **中文里夹英文不统一** | "部署到 K8s 集群"+"用容器编排平台" | 专有名词用原文：Kubernetes、Docker、CI/CD |

---

### 追问策略

当用户描述模糊时，按以下优先级追问：

**第一层：规模感**
- "这个系统/项目有多大规模？多少用户/节点/机器？"
- "团队多少人？你是什么角色？"

**第二层：时间线**
- "从什么时候到什么时候？做了多久？"
- "有没有里程碑节点？分几期？"

**第三层：你的贡献**
- "你具体做了什么？如果换成别人，会有什么不同？"
- "最大的难点是什么？你怎么解决的？"

**第四层：结果**
- "做完之后有什么变化？能举一个数字吗？"
- "如果没做这个项目，会有什么损失？"

**第五层：技术细节**
- "用到了哪些技术栈？哪些是你主导选型的？"
- "架构上有什么特别的设计决策吗？"

---

### 项目组织策略（拆分 vs 合并）

项目该拆开还是保持大项，本质是**面试叙事策略**：

#### 大项目（umbrella）

一个项目覆盖完整故事线，多条要点展示不同维度。

```
✅ 叙事力强：「从 0 搭建整个 CI/CD 平台」比「写了个 WebShell」有冲击力
✅ 符合国内面试偏好（深度 > 广度）
❌ 要点太多，面试官可能只挑 1-2 条问
```

#### 小项目（split）

每个独立成果拆成单独项目，各 2-3 条要点。

```
✅ 聚焦单一技能，关键词匹配精准
✅ 面试官容易定位想问的方向
❌ 看起来像凑项目数，失去平台级叙事高度
```

#### 判断标准

| 情况 | 建议 |
|------|------|
| 平台级建设（CI/CD、测试体系） | **保持大项目**，要点间有叙事关联 |
| 一个项目混了多条能力线（测试 vs 部署 vs 运维） | **考虑拆**，让面试官能快速定位方向 |
| 要点只有 1-2 条 | 偏单薄，考虑补足或并入相关项目 |
| 要点 6 条以上 | 偏散，考虑拆或精简 |
| 经验 < 5 年 | 倾向 3-4 个扎实大项目，而非 6+ 个小项目 |

#### 边界规则

- **工作经历**：公司、部门、职位、时间、一句话核心职责
- **项目经历**：具体做了什么、技术栈、量化成果，不与工作经历重复
- 项目按公司分目录：`dist/projects/<公司>/<slug>.md`

---

### 要点整合原则

**一个要点 = 一个完整故事（做了什么 + 怎么做的 + 结果）。**

不要把一个完整想法拆成两条，读起来像断了气：

```
❌ 拆两条（产品和技术分离，读着别扭）：
   - 从 0 搭建 CI 消息通知中台，日处理 15000+ 条消息服务 400 人
   - 基于 Go 多 Worker + 消息队列削峰 + 飞书 API 限流器保证高吞吐

✅ 合并一条（一气道来）：
   - 从 0 搭建 CI 消息通知中台，通过 Webhook + MongoDB 订阅规则实现事件精准转发至飞书群，
     以 Go 多 Worker + 消息队列削峰 + API 限流器保证高吞吐，日处理 15000+ 条消息服务 400 人
```

**高密度要点的结构：**

```
做了什么     → 从 0 搭建 CI 消息通知中台
怎么做的(1)  → Webhook + MongoDB 订阅规则 → 精准转发
怎么做的(2)  → Go 多 Worker + 队列削峰 + 限流器 → 高吞吐
规模结果     → 日处理 15000+ 条消息服务 400 人
```

- 技术名词用 `+` 串联，紧凑不啰嗦
- 「从 0 到 1」本身已是结果，不必再写「解决了此前无法…」——除非那是一个明确的价值拐点
- 技术栈行只放核心，被要点替换掉的技术（如旧的全栈 React）要及时删

---

## 工作流二：生成简历 HTML

### 列出可用项目

```bash
cd /home/xzh/Markdown-Resume
python scripts/generate_resume.py --list-projects
```

### 选择项目生成

```bash
# 全部项目（默认）
python scripts/generate_resume.py

# 指定项目（格式：公司/项目名）
python scripts/generate_resume.py -p bytedance/gitlab-ci-platform,bytedance/model-testing-deployment

# 单个项目
python scripts/generate_resume.py -p huawei/storage-server-testing
```

### 导出 PDF

**首选 Chrome headless**（CJK 加粗正常，图标完整，无页眉页脚）：

```bash
# 先确保符号链接：dist/assets -> ../assets
cd /home/xzh/Markdown-Resume
python scripts/generate_resume.py
google-chrome-stable --headless --disable-gpu \
  --print-to-pdf=dist/Resume.pdf \
  --no-margins --no-pdf-header-footer \
  file:///home/xzh/Markdown-Resume/dist/Resume.html
```

**备选 weasyprint**（WSL2 无 CJK 粗体）：

```bash
python3 -c "import weasyprint; weasyprint.HTML('dist/Resume.html').write_pdf('dist/Resume.pdf')"
```

### 版本管理

`dist/` 是独立私有 git 仓库，每次改动后提交：

```bash
cd dist && git add -A && git commit -m "update: xxx"
```

- 不推送远端，不创建 remote
- 改坏了直接 `git revert`

---

## 编辑基本信息

直接编辑 `dist/resume-base.md`：
- 头部联系信息（含手机、邮箱）
- 个人信息 / 个人总结（3-4 句，突出核心竞争力和年限）
- 教育经历（硕士在前，学士在后）
- 工作经历（简要：公司+部门+职位+一行概括，不写项目细节）
- 技能清单（按类别分组：主力语言、辅助语言、平台工具、其他）

**个人总结参考公式：**

```
[X] 年 [领域]经验，专注 [核心能力]。主导过 [代表性成果]。擅长 [差异化能力]。带过 [规模]团队。
```

**工作经历 vs 项目经历的边界：**
- 工作经历：公司、部门、职位、时间、**一句话核心职责**
- 项目经历：具体做了什么、技术栈、量化成果、**不在工作经历中重复**

---

## 向后兼容

如果 `dist/resume-base.md` 不存在，脚本自动回退到旧的单文件模式（`dist/Resume.md`）。旧文件的 `RESUME_PATH` 环境变量仍然可用。

---

## 格式陷阱

- weasyprint 粗体和图标需 CSS hack（`resume.css` 已有处理）
- WSL2 无 CJK bold 字体，推荐 Chrome headless 导出 PDF
- 图标 `<img>` 宽度由 CSS 统一控制，不要在 markdown 中硬编码
- 嵌套列表用 4 空格缩进（脚本会标准化 2→4）

---

## 参考来源

本技能的方法论综合自：
- Google X-Y-Z 公式（Laszlo Bock 的简历建议）
- STAR/CAR 行为面试框架
- `resume-bullet-writer` / `resume-quantifier` / `tech-resume-optimizer` / `resume-ats-optimizer`（Paramchoudhary/ResumeSkills）
- 用户现有简历的实际风格和结构偏好
